save description
    local storage array of descriptions
    on set
        add to array
save chat
    local storage array of chats
        chat: string
        id: number - get last id from array of chats

    on chat change
        chats.find(c.id = id).chat = chat

save character
    name
    description
    parameters


set description
    choose from store
        list of descriptions
            on:click=select
            delete button